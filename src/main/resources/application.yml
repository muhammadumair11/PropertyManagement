spring:
  application:
    name: PropertyManagementApplication
  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://192.168.100.3:5432/propertymanagmentsystem
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

server:
  port: 8082


